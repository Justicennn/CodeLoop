# Discount Calculator

This small project calculates a total after applying a percentage discount.
For example, a discount value of `10` means ten percent, so a subtotal of
`100.00` should produce a discounted total of `90.00`.

Run the tests with:

```text
python -m unittest -v
```
